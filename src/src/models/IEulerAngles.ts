export interface IEulerAngles {
    /** 'x' is the roll, tilting sideways left and right */
    x: number;
    /** 'y' is the pitch, looking up and down  */
    y: number;
    /** 'z' is the yaw, turning left and right */
    z: number;
}
